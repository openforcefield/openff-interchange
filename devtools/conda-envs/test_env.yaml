name: test
channels:
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip
  - pydantic
  - pint
  - sympy
  - qcelemental
  - jax  # TODO: Make this optional?

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - sympy
  - pint
  - openmm
  - openforcefield>=0.7.2
