name: test
channels:
  - conda-forge
  - omina/label/beta

dependencies:
    # Base depends
  - python
  - pip
  - pydantic
  - pint
  - sympy
  - openff-evaluator

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

