name: test
channels:
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip
  - pydantic
  - pint
  - openmm >=7.5
  - openforcefield >=0.8.3
  - jax

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  - mypy
  - unyt
