name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pydantic
  - pint
  - sympy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

