name: test
channels:
dependencies:
    # Base depends
  - python
  - pint
  - pydantic
  - sympy
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

