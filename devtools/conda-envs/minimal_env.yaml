name: openff-system-minimal-env
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - pydantic
  - pint
  - openmm
  - openff-toolkit
  - jax
  - mdtraj
  - ele

    # Testing
  - pytest
